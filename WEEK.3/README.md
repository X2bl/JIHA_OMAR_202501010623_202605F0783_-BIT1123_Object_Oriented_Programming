# Week 3-4 - Inheritance and Polymorphism

- `Person.java` is the parent class.
- `Student.java` and `Lecturer.java` inherit from `Person`.
- `Main.java` runs the program.

The `introduce()` method prints a different message for each object.
