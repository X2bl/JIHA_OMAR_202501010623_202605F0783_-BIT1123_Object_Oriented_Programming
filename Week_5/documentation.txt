2.1 Why are the variables declared as private?

To protect the data and prevent direct access from outside the class.

2.2 What happens if the variables are declared public?

Anyone can access and modify the variables directly, making the data less secure.

2.3 Why do we use getters and setters?

Getters and setters provide controlled access to private variables and help protect the data.